//! # Generic Operations
//!
//! Defines 1 CIR operation:
//!
//! - `generic_apply` — instantiate a generic function or type with concrete
//!   type arguments, producing a monomorphized version

use mlif::Context;

/// Register the generic_apply op with the given context.
pub fn register_ops(_ctx: &mut Context) {
    todo!()
}
