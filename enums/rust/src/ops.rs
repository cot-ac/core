//! # Enum Operations
//!
//! Defines 2 CIR operations:
//!
//! - `enum_constant` — create an enum value from a variant name
//! - `enum_value` — extract the underlying integer tag value from an enum

use mlif::Context;

/// Register all 2 enum ops with the given context.
pub fn register_ops(_ctx: &mut Context) {
    todo!()
}
